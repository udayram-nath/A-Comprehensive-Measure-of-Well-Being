# Model Files in Project

```
Flask/
├── model.pkl      # Trained LinearRegression model
├── encoder.pkl    # LabelEncoder for Country names
├── app.py         # Flask application
└── templates/
    ├── home.html
    ├── indexnew.html
    └── resultnew.html
```

Both `.pkl` files are required for the Flask app to serve predictions.
